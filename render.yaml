services:
  - name: miffuturo-backend
    type: web
    env: python
    region: virginia
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: CORS_ORIGINS
        value: https://miffuturo.onrender.com,http://localhost:3000
      - key: FRONTEND_ORIGIN
        value: https://miffuturo.onrender.com
  - name: miffuturo
    type: web
    env: node
    region: virginia
    rootDir: frontend
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    envVars:
      - key: API_HOSTPORT
        fromService:
          type: web
          name: miffuturo-backend
          property: hostport
      - key: API_ORIGIN
        value: https://miffuturo-backend.onrender.com
